# Avatar-CRUD
CRUD stands for Create, Read, Update, Delete — the four basic operations used to manage data in a databaseThese operations form the foundation of most software systems that interact with a database, typically mapped to HTTP methods in REST APIs: or application.
